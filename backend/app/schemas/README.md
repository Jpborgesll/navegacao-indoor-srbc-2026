# Schemas Module

Contém os modelos de dados utilizados para validação de entrada e saída da API.

- Utiliza **Pydantic** para garantir a integridade dos dados e gerar a documentação automática (Swagger).
- `route.py`: Definições para as requisições e respostas de cálculo de rota.
